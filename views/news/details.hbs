{{>navbar}}
 
<div class="container my-3 d-flex">
    <div class="col-2"></div>
    <div class="col-8">
            <h1 class="mt-3">{{news.title}}</h1>
            <p class=""><small class="">{{news.short_description}}</small></p>
            <p class="">{{{news.article_content}}}</p>
    </div>
    <div class="col-2"></div>
</div>

{{>footer}}